# Core dependencies
transformers==4.57.6
safetensors
einops
numpy
Pillow
tqdm
sentencepiece

# Video/Image processing
imageio
imageio[ffmpeg]
opencv-python
decord

# Model downloading
huggingface_hub
modelscope

# Evaluation metrics
lpips
scikit-image
torchmetrics

# 3D reconstruction
plyfile
jaxtyping
scipy
matplotlib
evo
e3nn
addict
moviepy==1.0.3

# Demo
gradio
trimesh
viser

# Training
accelerate
deepspeed==0.16.7
omegaconf
peft
tensorboard
wandb

# Text processing
ftfy

# Dataset processing
pandas

# Hand visualization (MANO)
smplx
projectaria-tools

# HaMeR baseline (eval_hamer_baseline.py)
# Cannot be installed via a plain pip URL — detectron2 and xtcocotools must be
# removed from its setup.py first.  Run once after cloning the repo:
#
#   git clone https://github.com/geopavlakos/hamer models/hamer/repo
#   sed -i '/detectron2/d' models/hamer/repo/setup.py
#   sed -i '/xtcocotools/d' models/hamer/repo/setup.py
#   pip install -e models/hamer/repo --no-build-isolation
